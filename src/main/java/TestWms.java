import static com.codeborne.selenide.Selenide.open;
import static io.qameta.allure.Allure.step;

public class TestWms {
    public void Test() {

        step("Модалка создания/редактирования маршрутов", () -> {
        });

        step("Поиск по городам,ПВЗ", () -> {
        });

        step("Выбор чек-боксов с левой стороны", () -> {
        });

        step("Списки ПВЗ городов (правая таблица)", () -> {
        });

        step("Номера маршрутов", () -> {
        });

        step("Стены упаковки", () -> {
        });

    }
}
